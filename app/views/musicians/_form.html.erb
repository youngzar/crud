<%= form_for(@musician) do |f| %>
  <% if @musician.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@musician.errors.count, "error") %> prohibited this musician from being saved:</h2>

      <ul>
      <% @musician.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :songs %><br>
    <%= f.text_area :songs %>
  </div>
  <div class="field">
    <%= f.label :monthly_listeners %><br>
    <%= f.number_field :monthly_listeners %>
  </div>
  <div class="field">
    <%= f.label :fans %><br>
    <%= f.number_field :fans %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
